# Progress — BioLiminal L2 Plan 2 — Wave F Blocked on Fixture Generator

## Session Directive
> **MANDATORY:** This progress file defines your assignment. Do NOT interpret, summarize, or re-scope.
> Resume work exactly where the previous session left off. Follow the What's Next section literally.
> If the plan or instructions are unclear, ask the user — do not guess or improvise.
>
> **CRITICAL:** Plan 2 execution is 18/19 tasks complete. Wave F task F1 hit STATUS: CHECKPOINT —
> 2/3 integration tests pass, 1 fails because the `overhead_squat_clean` synthetic fixture produces
> `mean_knee_valgus_deg ≈ 22.998` which crosses both 8.0 / 12.0 SBL thresholds, causing the
> `test_overhead_squat_clean_produces_no_observations` test to see a FLAG observation on a clean run.
> Root cause is in `software/server/tests/fixtures/synthetic/generator.py` geometry, NOT the reasoning
> logic. You must fix the generator, regenerate `overhead_squat_clean.json`, then re-run F1 and
> proceed to Wave G.

## Application Context
BioLiminal — UT Austin capstone. Phone-camera pose-estimation movement screening tool (free tier) +
future sEMG garment (premium). Wellness positioning only. The free-tier server is Python 3.11 /
FastAPI / pydantic v2 with an analysis pipeline that produces chain-reasoning reports. The current
milestone is the **Analysis Pipeline Epoch**, L1 deliverable `POST /sessions` → `GET /sessions/{id}/report`.
Plans 1, 4 complete. Plan 2 (chain reasoning + report assembly) is executing via parallel-plan-executor.
Plans 3, 5 are stubs.

## Position
- **Project:** Capstone (BioLiminal)
- **Repo path:** `/home/context/olorin/projects/bioliminal/RnD_Server/` (symlink — use this, NOT the direct path)
- **Remote:** `https://github.com/AaronCarney/capstone-fascia.git`
- **Branch:** `main`
- **HEAD:** `adf3841 test(integration): end-to-end report generation from synthetic fixtures`
- **Phase:** Plan 2 execution, Wave F checkpoint (F1 blocked on fixture generator bug)
- **Tests:** 152 unit tests passing. 2 of 3 F1 integration tests passing.

## L1 Context
- **L1 plan:** `docs/plans/2026-04-10-analysis-pipeline-epoch.md`
- **Epoch order:** 1 → 4 → 2 → 3 → 5 (Plans 1 ✓, 4 ✓, 2 in progress, 3/5 stubs)
- **Exit gate:** All 5 L2 plans + epoch integration test + decision document.

## L2 Context
- **L2 plan:** `docs/plans/2026-04-10-L2-2-chain-reasoning.md` (patched & approved through plan-review,
  19 tasks across 8 waves, max parallelism 6)
- **Execution strategy:** parallel-plan-executor (already dispatching waves — do NOT re-invoke
  plan-review; patches and review are complete)

## Task State (JSON)

```json
{
  "plan_id": "2026-04-10-L2-2-chain-reasoning",
  "plan_path": "docs/plans/2026-04-10-L2-2-chain-reasoning.md",
  "current_head": "adf3841",
  "test_baseline": {"suite": "software/server/tests/unit", "passing": 152},
  "waves_complete": ["0", "A", "B", "C", "D", "E"],
  "wave_in_progress": "F",
  "wave_f_state": {
    "F1": "CHECKPOINT — test file committed (adf3841), 2 of 3 tests pass. 1 test fails because overhead_squat_clean fixture produces mean_knee_valgus_deg=22.998.",
    "F2": "COMPLETE — committed f8f3486, 1 test passing"
  },
  "waves_remaining": ["F-finish", "G"],
  "commits_landed": [
    "c63bc0e PRE1 pyyaml dep",
    "020984c A1 ChainObservation feat",
    "d59e580 A1 additional tests",
    "96d3e22 A2 BodyTypeProfile",
    "aabe96b A2 hypermobile test",
    "ae4b303 A3 config schemas",
    "690285f A4 threshold YAML",
    "c6310d6 A5 SBL rule YAML",
    "84b0682 A6 BFL + FFL rule YAML",
    "c96f45e B1 threshold_loader",
    "34af16f B2 rule_loader",
    "07fdd2b B3 report schemas",
    "a5c8cd0 B4 chain_observations artifact field",
    "065ee97 C1 RuleBasedChainReasoner",
    "a4946f8 D1 run_chain_reasoning stage fn",
    "3fc5c93 D2 report assembler",
    "2fbeba9 E1 orchestrator wiring",
    "429cd68 E2 reports route Report shape",
    "f8f3486 F2 wellness language lint",
    "adf3841 F1 integration tests (2/3 passing — checkpoint)"
  ],
  "e2_deviations_from_plan": {
    "create_app_import": "auralink.api.main (not auralink.api.app per spec)",
    "env_var": "AURALINK_DATA_DIR (not AURALINK_SESSIONS_DIR per spec; Settings has env_prefix='AURALINK_' + data_dir field; sessions_dir is a computed_field)"
  },
  "f1_blocker_details": {
    "failing_test": "software/server/tests/integration/test_full_report.py::test_overhead_squat_clean_produces_no_observations",
    "observed": "mean_knee_valgus_deg ≈ 22.998 on clean fixture",
    "thresholds": {"concern": 8.0, "flag": 12.0},
    "root_cause": "fixture generator _frame_for_knee_angle() places ankle at (knee.x + r*sin(angle_from_down) + valgus_offset, knee.y + r*cos(angle_from_down)). As knee_flexion sweeps 90..180..90, the ankle x-coordinate sweeps horizontally, which the per_rep valgus metric (computed from left_knee_valgus / right_knee_valgus angles in pose/joint_angles.py) picks up as frontal-plane valgus even though knee_valgus_deg=2.0 was the intent.",
    "fix_options": [
      "OPTION 1 (correct): Move the knee-flexion arc into the sagittal plane by sweeping ankle y/z instead of x. Update _frame_for_knee_angle so ankle.x stays at knee.x + valgus_offset (no horizontal arc) and ankle.y = knee.y + r*cos(angle_from_down), and set ankle.z = r*sin(angle_from_down). Regenerate overhead_squat_clean.json.",
      "OPTION 2 (not recommended): Tune thresholds upward. Rejected — thresholds are research-backed (Hewett 2005)."
    ],
    "recommended_option": 1,
    "file_to_edit": "software/server/tests/fixtures/synthetic/generator.py::_frame_for_knee_angle",
    "regenerate_cmd": "cd software/server && uv run python -c \"from tests.fixtures.synthetic.generator import generate_session; import json; json.dump(generate_session('overhead_squat', injected_compensations={'knee_valgus_deg': 2.0, 'trunk_lean_deg': 4.0}), open('tests/fixtures/synthetic/overhead_squat_clean.json', 'w'), indent=2)\""
  }
}
```

## What's Next

You are resuming mid-execution. Do NOT restart from scratch. Do NOT re-run plan-review. Do NOT
re-dispatch completed waves.

### Step 1 — Verify state
```bash
cd /home/context/olorin/projects/bioliminal/RnD_Server && git log --oneline -5
# Should show adf3841 at top
cd software/server && uv run pytest tests/unit -q 2>&1 | tail -3
# Should show 152 passed
uv run pytest tests/integration/test_full_report.py -v 2>&1 | tail -20
# Should show 2 passed, 1 failed (overhead_squat_clean)
```

### Step 2 — Fix the fixture generator (F1 blocker)

Read these files first to ground the fix:
```
Read: software/server/tests/fixtures/synthetic/generator.py
Read: software/server/src/auralink/pose/joint_angles.py  # where knee_valgus_angle() lives
Read: software/server/src/auralink/pipeline/stages/per_rep_metrics.py  # mean_knee_valgus_deg
Read: software/server/src/auralink/pipeline/stages/angle_series.py  # feeds left/right_knee_valgus
```

The fix is in `generator.py::_frame_for_knee_angle`. Current geometry (lines 29–35):
```python
hip = (0.5, 0.5)
knee = (0.5, 0.7)
r = 0.2
angle_from_down_rad = math.radians(180.0 - knee_flexion_deg)
valgus_offset = math.radians(knee_valgus_deg) * 0.1
ankle_x = knee[0] + r * math.sin(angle_from_down_rad) + valgus_offset
ankle_y = knee[1] + r * math.cos(angle_from_down_rad)
```

The `r * sin(angle_from_down)` term puts the knee-flexion arc in the x-axis — the same axis that
knee_valgus_angle() reads as frontal-plane motion. Fix:

1. Put the knee-flexion arc into the z-axis (sagittal depth), NOT x:
   ```python
   ankle_x = knee[0] + valgus_offset
   ankle_y = knee[1] + r * math.cos(angle_from_down_rad)
   ankle_z = r * math.sin(angle_from_down_rad)  # was landing in ankle_x
   ```
2. Update the landmark construction to pass `z=ankle_z` to `_landmark(ankle_x - 0.05, ankle_y)` →
   you'll need to either extend `_landmark` to accept z, or inline `{"x":..., "y":..., "z": ankle_z, ...}`.
3. Regenerate `overhead_squat_clean.json` (and optionally `overhead_squat_valgus.json`,
   `single_leg_squat_clean.json`, `push_up_clean.json`, `rollup_clean.json` — verify each still makes
   sense after the change, especially the `valgus` variant which should still produce an SBL
   observation).
4. Commit: `fix(fixtures): move knee-flexion arc to sagittal plane so valgus metric stays clean`

**IMPORTANT:** Before regenerating all 5 fixtures, first regenerate only `overhead_squat_clean.json`
and re-run F1. If `test_overhead_squat_clean_produces_no_observations` passes AND the valgus test
still passes with the existing `overhead_squat_valgus.json` fixture (which should — valgus fixture's
valgus_offset is larger), you're done. Then regenerate the valgus fixture too for consistency and
re-run the full F1 suite.

If the Plan 4 unit tests for the existing fixtures break after regeneration (e.g., tests that
snapshot per_rep_metrics values), update those expectations — they were baselined against the
buggy geometry.

### Step 3 — Re-run F1
```bash
cd software/server && uv run pytest tests/integration/test_full_report.py -v
```
Expected: 3 passed. If still failing, STATUS: BLOCKED with the new observed value.

### Step 4 — Dispatch Wave G (final validation)

Wave G is a single-task wave (G1, skip-tdd). Dispatch via parallel-plan-executor or inline:

1. `cd software/server && uv run pytest -q` — expect ~180 tests pass (136 baseline + ~44 new).
2. `cd software/server && uv run ruff check . --fix && uv run ruff check .` — clean.
3. `cd software/server && uv run black . && uv run black --check .` — clean.
4. Re-run pytest — same ~180 tests pass.
5. Dev-server smoke: see plan section G1 — starts uvicorn on port 8765, POSTs valgus fixture, GETs
   report, asserts SBL observation. The heredoc is already corrected to terminate `PY` at column 0.
6. If ruff/black changed anything, commit: `chore(server): ruff/black cleanup after plan 2`.

### Step 5 — Finish
Invoke `finishing-a-development-branch` per no-push constraint → keep-as-is on main. Then proceed
to Plan 3 per epoch order (1 ✓ → 4 ✓ → 2 → 3 → 5).

## Key Files

### Plan files
- `docs/plans/2026-04-10-L2-2-chain-reasoning.md` — L2 Plan 2 (patched, approved, executing)
- `docs/plans/2026-04-10-analysis-pipeline-epoch.md` — L1 epoch (parent)

### Code landed this session
All commits between `c63bc0e` and `adf3841` — see `commits_landed` JSON array above.

### Blocked file
- `software/server/tests/fixtures/synthetic/generator.py` — the `_frame_for_knee_angle` function
  geometry needs the fix described in Step 2 above.

### Pre-existing untracked files (IGNORE, NOT in scope)
`docs/research/`, `hardware/`, `docs/operations/`, `README.md`, `docs/BRAINLIFT.pdf`,
`docs/decisions/`, `docs/file-inventory.md` — teammate work. Never stage or commit them.

## Git State

```
$ git log --oneline -5
adf3841 test(integration): end-to-end report generation from synthetic fixtures
f8f3486 test(reasoning): enforce wellness-language lint on rule narratives
429cd68 feat(api): GET /sessions/{id}/report returns structured Report
2fbeba9 feat(pipeline): wire run_chain_reasoning into default + push_up stage lists
a4946f8 feat(pipeline): add run_chain_reasoning stage function
```

Working tree clean on Plan 2 scope.

## Constraints/Blockers

### Critical
- **Do NOT re-invoke plan-review** — it already passed. The L2 plan is frozen for this execution pass.
- **Do NOT re-dispatch completed waves (0, A–E, F2).** Only F1 needs re-running after the generator fix.
- **Symlink rule:** access Capstone via `projects/bioliminal/RnD_Server/`, NOT `/home/context/projects/bioliminal/RnD_Server/`.
- **Summary files are gitignored** — do NOT try to `git add docs/sessions/summaries/task-*.md`.
- **Pre-existing untracked files NOT in scope** — ignore teammate files listed above.

### E2 deviations from plan (already landed — do NOT re-patch)
- `from auralink.api.main import create_app` (spec said `auralink.api.app`)
- `AURALINK_DATA_DIR` env var (spec said `AURALINK_SESSIONS_DIR`)

### Architectural facts (verified, do not re-investigate)
- `_frame_for_knee_angle` places ankle with x-axis arc — root cause of the F1 failure.
- `knee_valgus_angle` in `pose/joint_angles.py` computes from frontal-plane (x) geometry.
- Thresholds at `software/server/config/thresholds/default.yaml` are research-backed — do NOT tune them up to sidestep the fixture bug.

### Environment
- WSL2 Ubuntu; git user: Aaron Carney
- Python server: `cd software/server && uv run pytest -q`
- Test counts: 152 unit passing (was 136 baseline from Plan 4). Integration: 2/3 passing on F1.

---

Checkpoint at 28% context during Wave F. Plan 2 execution is 18 of 19 tasks committed. Next session
must fix the fixture generator, re-run F1, then dispatch Wave G, then invoke finishing-a-development-branch.

--- End Progress ---
